# Launch audit work log

This file records issues discovered during the evidence-based launch audit.
An issue is closed only after its stated verification passes. `BLOCKER` means a
launch decision or external action is required and must not be guessed.

| ID | Severity | Status | Issue and evidence | Resolution | Verification |
|---|---|---|---|---|---|
| LAUNCH-093 | P2 | Resolved | Recurring coverage was discoverable only inside an individual Article Record. A reader had to choose a note before seeing which subjects had a recent published update. AIMA's 2026 emerging-manager survey stresses operational quality, while FactSet's research-workflow analysis describes curated updates and research recall as investment-team needs. | Added a restrained four-subject Coverage Monitor to the owner home, ordered by latest publication with exact archive-derived evidence counts and direct entry to source history. Preserved dependency-free generation, lazy loading, and local review boundaries. Recorded buyer evidence and a paid-pilot validation method in the subscription playbook. | The 555-test suite, tracked-data validation, complete release validation, inline-script syntax, Python compilation, Ruff, mypy, shell/plist syntax, and diff checks passed. Runtime regression covers chronology, missing references, singleton exclusion, double-count prevention, source escaping, and exact navigation with stale filters cleared. Browser QA at 1,280, 768, 375, and 320 px found zero horizontal overflow; mobile home targets were at least 44 px. Light/dark layouts, mouse entry to Two Sigma history, and keyboard entry to Bonds history passed with no console errors. Commit `00fb3aa8d0fffa0f2541b75ca56db8caa7af5038` passed **Validate and Deploy Pages** run `33963997200`, exact live smoke, CodeQL, and scheduled watchdog run `33964334370`; production browser QA confirmed revision `00fb3aa8d0fffa0f2541b75ca56db8caa7af5038`. |
| LAUNCH-001 | P2 | Resolved | Repository had no `AGENTS.md` or `ISSUES.md`, so build/test/lint expectations and audit findings were not durably documented. | Added repository-wide working instructions and this issue log; corrected the temporary build example so one output directory is reused and removed. | Full 158-test suite and Python/shell/plist syntax checks passed after creation. |
| LAUNCH-002 | P2 | Resolved | `ruff check *.py` found three code-quality errors: an unused `datetime` import, an unused `has_quant` assignment, and an f-string without substitutions. | Removed the two dead values and converted the static f-string to a plain string; no runtime behavior changed. | Audit baseline passed 158 tests, Ruff, tracked and strict data validation, Python/shell/plist syntax, deterministic build, and inline-script compilation. |
| LAUNCH-003 | P1 | Resolved | `refresh.sh` let `llm_direction.py` mutate tracked `.direction_cache.json` before validation and could leave a mixed snapshot if a promotion `mv` failed. | Candidate cache and all promoted files now participate in one rollback transaction. Rollback remains armed through the local commit, so staging/commit failures restore the prior snapshot; a push failure retains the clean local commit for retry. | Validation, regression, promotion, Git-add, and Git-commit fault injection preserved the prior files and cleared staging/lock/temp state as applicable; the next run retried successfully and the full 177-test suite passed. |
| LAUNCH-004 | P1 | Resolved | `automation_status.sh` reported healthy while `launchctl` showed the updater's latest exit code was 1. | Status parsing now treats a non-zero latest LaunchAgent exit as unhealthy and preserves diagnostic output. | Fake-launchctl coverage and the full 177-test suite passed; a real LaunchAgent refresh on 2026-07-21 completed with latest exit 0, and `automation_status.sh` accepted the resulting snapshot. |
| LAUNCH-005 | P2 | Resolved | Medium RSS used an unbounded remote read and parsed XML without rejecting DTD/entity declarations or an off-origin redirect. | RSS bytes are capped at 2 MB; unsafe XML declarations and non-HTTPS/non-Medium final URLs are rejected before parsing. | Ten Medium tests passed and prove parsing is never reached for oversized, non-UTF8, DTD/entity, non-HTTPS, or off-origin responses; targeted S314 scan passed. |
| LAUNCH-006 | P2 | Resolved | Production-only mypy reported 17 errors across eight files, and CI did not run Ruff or mypy. | Added minimal annotations/narrowing, a stable production mypy configuration, and pinned CI quality tools. | Ruff and mypy report zero errors locally and in the clean **Validate and Deploy Pages** run for `551ce56c345f97e95239eb132e7a90a515af06ce`. |
| LAUNCH-007 | P2 | Resolved | When both clipboard APIs failed, the UI removed the temporary text and told the reader to copy text that was no longer available. | Added an accessible modal that preserves, focuses, and selects the complete text for manual copy. | Both focused clipboard/queue regression tests and generated inline-script compilation passed. |
| LAUNCH-008 | P2 | Resolved | Launch date, Evidence Monitor naming, Medium network prerequisites, and the clean-clone validation command were inconsistent across owner docs. | Updated README, runbook, and requirements; tracked validation is now the default and strict cache binding is documented separately. | Documentation consistency and launch-readiness tests passed. |
| LAUNCH-009 | P2 | Accepted privacy exception | The checklist asks for analytics events, but the product deliberately ships no reader-level analytics or telemetry. Network behavior is constrained to release-bound same-origin assets. | Keep privacy-first measurement: GitHub aggregate repository traffic plus owner-managed Search Console. Adding behavioral tracking requires a separate product/privacy decision. | Source/CSP/network audit found no analytics SDK, pixel, beacon, cookie, or third-party data submission; this exception is accepted for launch. |
| LAUNCH-010 | BLOCKER | Resolved; optional owner monitoring follow-up | GitHub project Pages cannot place `robots.txt` at the `navnoorthapar.github.io/robots.txt` origin root, so crawlers resolving the origin-root path received a 404, and no sitemap was advertised there. Search Console ownership was also not evidenced in this repository. | The separately owned `navnoorthapar.github.io` user-site repository now serves a permissive origin-root `robots.txt` advertising this release's sitemap (commit `16e0349`). Sitemap discovery no longer depends on Search Console: the `Sitemap:` directive is the standard crawler-discovery mechanism and is now published at both the origin root and the project path, alongside the existing meta robots, canonical metadata, and project sitemap. Search Console ownership remains available to the owner for index-coverage **monitoring** and manual recrawl requests; it is an observability preference, not a discovery or indexing dependency, so it is no longer tracked as a launch action. | `https://navnoorthapar.github.io/robots.txt` returns HTTP 200; `urllib.robotparser` confirms it disallows nothing across all seven project paths and exposes the sitemap. Both robots files advertise the same sitemap URL, which returns HTTP 200 and parses as valid XML with 476 `<url>` entries. The terminal, its project robots file, and its sitemap all return HTTP 200 on the live release. Deliberately no build change was made for Search Console verification: adding an unused code path to a byte-exact-validated release pipeline on launch eve carries more risk than the optional monitoring it would enable. |
| LAUNCH-011 | P3 | Accepted platform boundary | GitHub Pages does not expose custom response-header configuration for CSP, X-Content-Type-Options, or frame policy. | Retain exact-hash meta CSP, no-referrer policy, early frame clearing, HTTPS enforcement, and the documented hosting boundary. | Live response and generated-document security audit completed; no high/critical vulnerability found. |
| LAUNCH-012 | P2 | Resolved | Browser error paths needed executed evidence for malformed input, network/deferred-asset failure, empty results/queue, unavailable storage, and stale-shell recovery. | Added dependency-free Node runtime tests that execute the generated client functions for oversized/malformed imports, HTTP failure/timeout, unavailable session storage, and one-shot stale-shell recovery; queue empty state was browser-verified. | All four client runtime tests passed; each preserves data or emits the bounded recovery message, and the empty queue presents restore/browse actions. |
| LAUNCH-013 | P2 | Resolved | The prior launch record referred to an older commit and did not contain final responsive, accessibility, performance, deployment, or automation evidence. | Added the dated 2026-07-21 completion record. The protected `launch-2026-07-21` annotation is the authoritative record of the final release SHA and exact deployment/watchdog run URLs, avoiding a self-referential documentation commit. | The launch tag is created only after the tagged commit's exact-production smoke, final watchdog, and all launch checks pass. |
| LAUNCH-014 | P3 | Accepted architecture boundary | Hash-selected views share one canonical HTML document, generic social metadata, and one social image. | Preserve the fast single-document terminal; individual article snippets/unfurls would require path-based prerendered routes and a different publishing architecture. | Canonical document, Open Graph/Twitter metadata, JSON-LD, sitemap, manifest, and support assets validate on every hash view. |
| LAUNCH-015 | P1 | Resolved | Representative production Lighthouse found the Evidence Monitor at 89 performance because it initially rendered 100 dense rows; the other routes were 95–98. | Reduced the initial evidence batch to 50 while preserving explicit incremental loading. | Exact-production Lighthouse performance is 98 Latest, 93 Evidence, 93 Library, and 92 Queue; Best Practices and SEO are 100 on every route. |
| LAUNCH-016 | P1 | Resolved | Lighthouse found an unlabeled file input, a skipped heading level, and insufficient mobile text coverage; accessibility was 95–96. | Added an explicit file-input label, a semantic evidence-section heading, and 12 px minimum mobile data/filter text. | Exact-production Lighthouse accessibility is 100 with zero failing accessibility audits on Latest, Evidence, Library, and Queue. |
| LAUNCH-017 | P2 | Resolved | A Git staging or commit failure after snapshot promotion left validated files dirty or staged, so the next scheduled refresh failed its clean-worktree gate instead of retrying. | Kept the promotion rollback armed through the local commit and explicitly reset the publication paths after restoring the prior snapshot; push failures still retain the clean local commit for retry. | Executable fault-injection tests prove both `git add` and `git commit` failures restore exact prior bytes, clear staging/temp/lock state, and allow the next run to publish successfully; the 177-test suite passed. |
| LAUNCH-018 | P2 | Resolved | The visually hidden skip link was repeated at the bottom of every printed page and overlapped article content. | Added the skip link to print-only terminal chrome suppression. | A real six-page headless-Chrome PDF was rendered through Poppler; inspected pages 1–3 contain no overlay or clipped content, and the print regression test passes. |
| LAUNCH-019 | P2 | Resolved | The launch tag was designated as the authoritative certification record, but no repository rule prevented a matching tag from being moved or deleted. | Added active tag ruleset `19448243` for `refs/tags/launch-*`, blocking updates and deletion while allowing a new launch tag to be created. | The repository API reports target `tag`, active enforcement, update/deletion rules, the exact launch pattern, no bypass actors, and `current_user_can_bypass: never`; the final remote tag object and peeled commit are verified after creation. |
| LAUNCH-020 | P2 | Resolved | The responsive record overstated a blanket 44 px tablet target floor, and exact hit testing found the desktop inspector close control fractionally below 24 px plus mobile drawer text controls without an explicit 44 px minimum. | Gave the inspector close control a 24 px baseline and 44 px mobile width, extended the 44 px mobile height rule to drawer text controls, and corrected the certification language to distinguish mobile touch sizing from compact tablet density. | Generated-CSS regressions enforce the new dimensions; exact-production hit testing at 375, 768, and 1,440 px must show no overflow, no visible non-inline target below 24 px, and a 44 px mobile floor. |
| LAUNCH-021 | P1 | Resolved | The tracked article catalogue was not published under `/data/`; the required article index, latest feed, and dataset manifest returned the Pages 404 document. | Added six deterministic, project-relative public JSON endpoints, exact master-catalogue copying, bundle checksums, semantic validation, and contract documentation. | Local validation and exact builds passed; production serves all six endpoints as JSON, the public article index is byte-identical to the 458-record tracked catalogue, and release `adae777ecf4aafa677399f1f82bb29873782cb59` passed exact-release smoke with data digest `543defa34829f2bdbac4b16980ac4dcf24e91ebe7f5c714e9d3029c1e0dac69d`. |
| LAUNCH-022 | P1 | Resolved | Downstream research automation had no fast entity lookup or explainable related-article graph. | Added a deterministic curated-entity/capitalized-phrase search index and field-weighted TF-IDF related graph whose reasons contain only terms actually shared by each pair. | Corpus-wide determinism, relevance, no-fabricated-reason, and size tests passed; production serves the 211,928-byte search index and a top-five related list for every one of the 458 catalogue records. |
| LAUNCH-023 | P2 | Resolved | Articles had no stable topic-family label, so downstream planning could not learn or balance recurring research formats. | Added the seven-value, priority-ordered deterministic classifier, a committed hand-labeled acceptance fixture, and the family endpoint. | The 20-article fixture has full agreement, exceeding the 85% gate; all 458 catalogue records classify exactly once and the production family totals sum to 458. |
| LAUNCH-024 | P1 | Resolved | The archive omitted the author's public Patreon metadata and verified FX Empire bylines. Public sparse Patreon metadata was available without authentication, and the tracked verified FX Empire registry contained 11 bylines. | Added a metadata-only Patreon collector, tracked Patreon and FX Empire registries, conservative cross-source matching, manual overrides, exact-empty-body enforcement for registry-only records, and transactional refresh integration. | URL, access-flag, deduplication, cross-link, privacy-corruption, and rollback tests passed; the release-bound manifest records 230 Substack, 135 Medium, 82 Patreon, and 11 FX Empire records, with all four sources healthy and no paid body republished. |
| LAUNCH-025 | P2 | Resolved | Hash-selected dossiers shared generic social metadata and had no per-article static link card. | Added deterministic 1200×630 PNG cards and lightweight `/a/` Open Graph stubs for every catalogue record while preserving the terminal's existing hash-selected flows. | Build validation proves complete 458-card/458-stub coverage, exact 1200×630 dimensions, and 458 sitemap stub URLs; exact-production smoke validated representative content-bearing and registry-only card/stub pairs, and the production share digest is `5f8b1dcaa26ab9664b073c2c4ca79bd7326a0c5819b87ce01761e95c770eb186`. |
| LAUNCH-026 | P1 | Resolved | CI, the watchdog, and deployment smoke knew only the legacy eight-file artifact and did not validate the new data contract or prohibit private-analytics keys structurally. | Replaced obsolete whole-artifact assumptions with per-surface budgets, every-contract validation, normalized forbidden-key privacy checks, complete catalogue-to-card/stub validation, and exact-release verification for the data and support bundles. | Negative corruption tests and all local gates passed; **Validate and Deploy Pages** run `30165973459` passed quality, deployment, and exact live smoke for `adae777ecf4aafa677399f1f82bb29873782cb59`, then independent watchdog run `30167183732` passed exact-release and freshness checks. |
| LAUNCH-027 | P1 | Resolved | Individual dossiers exposed strong source evidence but made the reader reconstruct the author’s publication history manually, while MarketStack-style “history” or “coherence” labels would overstate what this single-author source corpus can support. | Added deterministic Research Threads from repeated high-precision topic keys, exact publication timestamps, a bounded chronology, extraction-role comparison, optional checksum-bound prior-passage comparison, validated hash navigation, responsive and print layouts, and explicit non-inference boundaries. No position, reversal, contradiction, conviction, accuracy, performance, or portfolio state is manufactured. | The 251-test launch suite passed; exact release `3595b78e219501c5d21aca973cc053941e24c1f1` passed deployment run `30282160787` and watchdog `30282363771`. Live browser checks proved topic switching, deferred exact-passage loading, owned chronology navigation, title focus, current snapshot status, and zero console errors. |
| LAUNCH-028 | P1 | Resolved | The first Research Threads implementation left the HTML release shell at 854,335 bytes, only 45,665 bytes below its 900 KB launch gate and roughly 33 average article additions from failure. | Added a deterministic compact article wire format that removes repeated derived/default fields and packs brief/coverage booleans; the client restores the exact existing runtime object before indexing or rendering. | Lossless hydration and reproducibility tests passed. The release gate measured 730,861 raw bytes and 189,688 gzip bytes—169,139 bytes of raw headroom—and exact-production smoke validated the resulting HTML and both release-bound deferred archives. |
| LAUNCH-029 | P1 | Resolved | Real-browser inspection found that a long single-line thread framing could give the timeline grid item an intrinsic width of about 2,398 px inside an 846 px column, placing the button center beneath the fixed evidence sheet and breaking pointer navigation. | Constrained each timeline grid item with `min-width: 0`, allowing the existing ellipsis rule to operate within the available research column. | Real-browser checks at 375, 768, 1,280, and 1,440 px found no root, body, or thread overflow; live production at 768 px also had zero overflow. Topic and chronology clicks completed, and chronology navigation focused the destination dossier heading. |
| LAUNCH-030 | P2 | Resolved | The real eight-page IC PDF clipped the final letter of the `CURRENT` comparison header because the 52 px compact column inherited the 12 px mobile/print text floor. | Widened the compact prior/current columns while retaining the three-column mobile/print comparison and its 12 px text floor. | Local regeneration and the exact-production seven-page PDF both preserved the complete `PRIOR` and `CURRENT` headers. Poppler text extraction and rendered-page inspection found no clipping, overlay, or horizontal truncation in the comparison or chronology. |
| LAUNCH-031 | P1 | Resolved | **Validate and Deploy Pages** run `30281599465` correctly blocked release `d07ee2921b88b96c0fbf88381a2f1acab7bd5443`: its artifact audit still required an explicit `idea_ids` list on every raw embedded article, while the newly validated compact wire contract deliberately omits empty lists and restores them before the first client consumer. No production deployment occurred. | Aligned the independent artifact audit with the compact contract: an omitted `idea_ids` value is normalized to an empty list, while any present non-list value still fails closed; deployment configuration regression coverage pins that behavior. | The focused 86-test surface and independent red-team review passed. Deployment run `30282160787` then passed the corrected integrity gate, exact-revision Pages smoke, and all size policies; watchdog `30282363771` independently rebuilt fingerprints and verified the exact live release and 8.4-hour snapshot freshness. |
| LAUNCH-032 | P1 | Resolved | The 2026-07-27 22:00 scheduled refresh exited 1 because Substack changed 212 paid catalogue rows to `body_html: null`; the old collector called `len(None)`. Live detail-endpoint review also proved that non-empty paid HTML can contain only a 0.9–2.5% teaser while retaining the full 2,333–4,111 reported word count, so accepting any non-empty detail as full would silently mislabel excerpts. Offset pagination, detail/list revision races, and cached bodies whose declared coverage changed also needed fail-closed handling. | Two complete ordered catalogue passes must match before body work, and cross-page overlaps fail closed. Only explicitly public text covering at least 97% of the list row's declared word count can be current full text; detail revision must exactly match the list revision; paid rows never request detail; and actual detail requests have a hard run ceiling. Cached bodies are rechecked against both cached and current declared coverage. Current, prior, and revision-unverified body captures retain separate tracked timestamps; historical evidence stays searchable but is visibly flagged and excluded from high-context eligibility. Medium fallback receives the same provenance treatment. | The complete 353-test suite passed on both supported Python runtimes. The transactional refresh produced 232 stable Substack rows without requesting paid detail, conservatively classified 19 current full bodies, 143 current excerpts, and 207 revision-unverified captures, and published the 2026-07-29 snapshot. Exact deployment run `30473834734`, watchdog run `30475113063`, and production browser checks passed. |
| LAUNCH-033 | P1 | Resolved | The compact article encoder, browser hydrator, tests, and a 274-line CI-only artifact script interpreted the same wire format independently. That drift caused run `30281599465`; moreover, scheduled refreshes could commit and push without building the exact Pages artifact, CI compared only `HEAD^`, and the shared validator initially bound IDs while leaving some derived article/observation values, ordering, inline dossier placement, and share bytes mutable. | Added one versioned Python wire contract and one executable release validator; wired it into the transactional refresh, exact-SHA detached pre-push gate, CI, and watchdog; changed regression comparison to the push event's prior remote SHA; and bound exact chronology, aggregates, masks, observation fields/order, inline/deferred dossier ownership, and every generated card/stub byte to source projections. | Adversarial dual-runtime tests, two complete exact-SHA release-gate executions, CI run `30473834734`, exact live smoke, and independent watchdog `30475113063` passed for `f8b423943e1a1929a04acee2d03791ecaf2fd880`. Production metadata reports 369 dossiers, 1,233 observations, and checksum `8c88f3cd2d2bfa93f3e103894b3e14a0ea243d0573a9c97363f0c62436adb9d8`. |
| LAUNCH-034 | P1 | Resolved | A regression/build process could mutate tracked release inputs after tests while the artifact retained the original SHA; rerunning an older successful workflow could deploy over newer `main`; the documented rollback rebuilt old snapshots, which the 16-hour freshness gate makes impossible precisely when a durable rollback is needed. The pre-push hook also allowed main deletion and did not prove fast-forward ancestry. | Recheck clean exact-HEAD state before and after building, reject stale remote-main deployments, remote-main deletion, and non-fast-forward pushes, and emit an incident annotation after any post-upload smoke failure. Every successful release retains a 90-day immutable rollback bundle containing exact site/source/social bytes, an attested payload digest, release-contract fingerprints, and an exact site path-to-hash map. A manual main-only workflow authenticates the original successful push or manual release run/SHA, safely extracts and verifies its stable schema-v2 attestation with current trusted tooling, refuses superseded `main`, redeploys exact bytes, and compares every live HTTPS file by path, length, hash, and bytes with a revision-bound cache key. Schema-neutral verification accepts valid evolved fingerprint key sets; historical release code is never executed in the privileged rollback job, and normal freshness policy has no bypass. | Deployment `30473834734` retained artifact `8732862778` through 2026-10-27. Same-release rollback drill `30475042604` authenticated that run/SHA, verified the schema-neutral attestation with current trusted tooling, refused superseded tooling, redeployed the identical artifact, and proved every live byte and revision in 38 seconds. Post-rollback watchdog `30475113063` passed exact-release and freshness enforcement. |
| LAUNCH-035 | P1 | Resolved | The release validator previously allowlisted and self-fingerprinted discovery/support files without proving their meaning; a `Disallow: /` robots file, incomplete sitemap, arbitrary manifest/favicon, or malformed replacement social image could become the newly trusted fingerprint. | Independently render and compare exact robots, full ordered sitemap, web manifest, and favicon bytes from source; require `og.jpg` to match the tracked image, satisfy JPEG framing/size policy, and be exactly 1200×630. Normal and rollback artifacts use the same proof. | Both supported Python runtimes reject meaningful mutations to every support asset and JPEG content/dimensions. Exact production validation in run `30473834734`, the post-deploy smoke, same-release rollback `30475042604`, and watchdog `30475113063` all accepted the independently rendered support bundle. |
| LAUNCH-036 | P1 | Resolved | Final adversarial review found three ways a nominally fresh release could overstate its evidence: two stable Medium archive passes could silently omit malformed rows, a `full` row could carry zero words or an empty-body brief, and a fresh manifest timestamp could mask source checks many hours or days old. RSS redirect validation also accepted another author's canonical Medium path. | Require exact canonical RSS and GraphQL destinations, bounded strict JSON, structurally complete unique archive rows, two identical archive passes, and retention of every prior Medium ID unless explicitly reviewed with `FORCE_FETCH`. Require positive word counts, a non-empty body and at least 97% declared coverage for full cached posts, plus a non-empty body digest for full public rows. Bind every source check to within one hour of the manifest and sixteen hours of the validation/watchdog clock. | The complete dual-runtime suite passed. Two identical Medium archive passes found 366 authored rows, conservatively removed 229 cross-posts, and retained 137 unique Medium articles. The tracked snapshot checked at `2026-07-29T16:54:40Z`; exact deployment `30473834734` and watchdog `30475113063` passed the one-hour source/manifest and sixteen-hour wall-clock freshness gates. |
| LAUNCH-037 | P1 | Resolved | Public Substack rows with a cached excerpt or legacy unverified body returned that cache before the bounded detail request, so one transient outage or request-budget miss could prevent the article from ever upgrading after the endpoint recovered. A formerly public row that became access-limited could also preserve a matching cache as `full`. | Only an exact same-revision current full cache may short-circuit a public detail request. Public excerpt, prior, unverified, and legacy caches now retry bounded detail and preserve their exact prior capture only when verification fails; access-limited caches never claim full content. | Focused recovery/downgrade tests and the complete 353-test suite passed on both supported Python runtimes. The real transactional refresh exercised the bounded retry path, preserved unverifiable historical captures without upgrading them, published the valid snapshot, and passed exact deployment `30473834734` plus watchdog `30475113063`. |
| LAUNCH-038 | P1 | Resolved | Final identity and fairness review found that Medium item URLs were not bound to one exact author path, title-only reviewed mappings could drift onto a different article, GraphQL paragraphs had no independent full-length proof, blank timestamps could accompany a `full/current` claim, and the fixed newest-first Substack detail budget could permanently starve row 13. | Canonical Medium identities now require the exact author, one strict UTF-8 canonical slug, and the trailing immutable post ID; RSS permits only its exact tracking-query shape and stores a query-free URL. Reviewed mappings bind Medium ID to Substack slug with title cross-checks. Every GraphQL body is an excerpt, full bodies require non-empty equal revision timestamps, and private oldest-attempted retry state rotates the bounded Substack detail budget. | The adversarial identity, Unicode, RSS, immutable-mapping, timestamp, GraphQL, and two-run fairness regressions and the complete 353-test dual-runtime suite passed. The refreshed 464-record catalogue, exact deployment `30473834734`, production provenance-filter/warning checks, and watchdog `30475113063` passed. |
| LAUNCH-039 | P2 | Resolved | The first exact-SHA pre-push rehearsal completed 352 of 353 tests but a normally millisecond-scale Node client-contract subprocess timed out at 10 seconds while competing with back-to-back deterministic site builds. The hook blocked the push and no production action ran. | Keep every Node regression subprocess bounded, but allow 30 seconds for cold-start/resource contention on the scheduled Mac. | The isolated client runtime test passed five consecutive times on each supported Python runtime; the complete exact-SHA release gate then passed twice, the pre-push hook allowed `f8b423943e1a1929a04acee2d03791ecaf2fd880`, and CI run `30473834734` repeated the full gate successfully. |
| LAUNCH-040 | P2 | Resolved | Final operations preflight found launchd loaded with the correct 09:00, 13:00, and 22:00 schedule but last exit 1 and a stale success marker. The 22:00 run had correctly failed closed because the development worktree was dirty; the manually completed July 29 transactional refresh and later exact push/deployment therefore had no matching local marker update. | After the exact refreshed snapshot was pushed, deployed, rolled back identically, and independently watched, reconcile the marker to that successful publication and immediately kickstart launchd. The existing 30-minute duplicate guard must exit before fetch, mutation, commit, or push. | Launchd run 18 logged `Refresh completed 0 minutes ago; skipping duplicate run.`, exited 0, and left local and remote `main` unchanged at `f8b423943e1a1929a04acee2d03791ecaf2fd880`. `automation_status.sh` reports the updater loaded, last exit successful, current refresh, and successful deployment. |
| LAUNCH-041 | P2 | Resolved | Scheduled watchdog `30476084356` started while release `1fa03fe0bda05b8e6ff8477077e89f7e37daf9af` was still in its quality gate. Its two attempts five seconds apart correctly saw the prior live bytes but exhausted before the healthy deployment completed, producing a false outage notification. Manual watchdog `30476349431` passed immediately after deployment. | Keep exact-byte and exact-revision enforcement unchanged, but give the independent watcher a bounded deployment-overlap window: 20 attempts with 20-second backoff, per-request 10-second timeouts, and a 12-minute job ceiling. A genuinely stale or unreachable release still fails closed after the bounded window. | Deployment-configuration regression coverage pins the overlap budget, exact URL/revision/digest checks, freshness gate, read-only permissions, absence of Git writes, at least six minutes of healthy stale-shell tolerance, and a worst-case first-request timeout budget inside the job ceiling. Corrected deployment `30477267963` and watchdog `30477267475` were dispatched in the same second; the watcher retained the expected new SHA through the 2m42s quality gate and 15s deployment, then passed exact-release and freshness verification in 3m14s without a false alert. |
| LAUNCH-042 | P2 | Resolved | GitHub history contained three intentionally red Dependabot runs (`30473982892`, `30473993019`, `30477417697`) because routine action-version PRs cannot update the separate reviewed SHA contract. The configured custom labels did not exist, producing a Dependabot configuration warning. Default concurrency could replace a pending production or watchdog run, a second rapid `main` push could make the older serialized run fail at its stale-release guard, and an older watchdog could fail after a newer `main` took ownership even though those supersessions are operationally healthy. | Upgrade the official checkout/setup actions to their verified current immutable tags; retain exact reviewed SHA enforcement; disable routine version PRs while leaving repository security updates enabled; create the configured labels; retry transient pinned-tool downloads; queue production/rollback/watchdog work without pending-run replacement; cleanly skip a valid superseded deployment before Pages; and reconcile a failed old watcher against exact current `main` while unchanged/unavailable authority still fails. | The committed and pre-push gates passed 362 tests plus the complete artifact/tooling suite. Exact deployment `30489757903` and an overlapping watchdog `30489765276` both passed for `bc41d6318945cbcdd0e743bb7a90ea52f99782a7`; the watcher safely observed ten pre-deploy propagation attempts and then accepted only the exact release. GitHub re-audit found no active/queued errors, open pull requests, Dependabot alerts, secret alerts, or check annotations; both configured labels exist and security updates remain enabled. |
| LAUNCH-043 | P1 | Resolved | Adversarial review of the first supersession fix found that a green quality-only superseded run still retained a rollback artifact and could satisfy the rollback workflow's run-level authority check without ever deploying or passing live smoke. It also found a final time-of-check/time-of-use window between the separate currency job and `deploy-pages`, while workflow-level `queue: max` unnecessarily queued obsolete pull-request revisions. | Scope cancellation to obsolete pull-request quality jobs, move the shared FIFO production queue to deploy/rollback jobs, recheck exact remote `main` immediately before `deploy-pages`, and make rollback authorization strict-read GitHub's run plus complete latest-attempt jobs metadata. Require exactly one successful `Deploy production` job and exactly one completed-success Pages deploy and exact-live-smoke step; reject skipped, failed, duplicate, foreign, stale-attempt, paginated, incomplete, or malformed evidence. | Eight adversarial verifier test groups reject every identified negative path, and independent code review found no remaining workflow blocker. Hosted deploy `30489757903` passed the new final currency guard and exact live smoke. Same-release rollback `30490039522` then authenticated that run's exact deploy/smoke steps, verified and redeployed identical attested bytes, and passed exact live-byte smoke; post-rollback watchdog `30490097582` passed exact release and freshness in 33 seconds. |
| LAUNCH-044 | P1 | Resolved | Enabling CodeQL's extended Python/Actions suite with remote-and-local threat modeling exposed 82 open alerts. The SSRF, URL, parser, and regex findings were remediated; 68 path findings were individually verified as intentional trusted local/operator paths and dismissed with durable boundary comments. | Bind Ollama to proxy-free loopback, structurally parse generated scripts, parse source hosts exactly, replace ambiguous regexes with linear structures, and cover them with adversarial regressions. | CodeQL run 30692490513 passed for exact commit `75a8a27a24dbe88ad80b0540ebc4a3ca4d0df0ff`: the last three findings closed automatically as fixed, no new finding appeared, and open alerts reached zero. Deployment run 30692490653 then passed isolated analysis, all 380 dependency-free tests, exact artifact checks, production deploy, and live smoke for the same commit. |
| LAUNCH-045 | P2 | Resolved | The artifact-producing CI job installed pinned but unhashed developer tools from PyPI before building the production Pages bundle, so a registry compromise could influence the untracked artifact without changing source. | Run Ruff and mypy in an isolated read-only job, then execute tests and build the release in a fresh dependency-free runner that performs no package installation. | Deployment run 30691866820 passed for exact commit `0505136d503d76c7b7b67c670a8ef23c769fadae`: isolated Ruff/mypy, 378 dependency-free quality tests, immutable-input and exact-artifact validation, rollback bundling, deploy, and exact live smoke all succeeded with zero annotations. |
| LAUNCH-046 | P2 | Resolved | Direct coverage was missing for the refresh-critical filter/deduplication stage. New tests found that removing only the words `hedge fund` could manufacture misleading underlyings such as `global` or `US`. | Drop the complete semicolon-delimited manager/noise fragment, mechanically recompute all 1,233 underlying fields from their exact visible passages, migrate the 25 affected observations, rebind the snapshot checksum, and cover filtering, fallback evidence, deduplication, date sorting, atomic replacement, and failure cleanup directly. | Nine focused filter tests, corpus-wide zero-drift derivation validation, the complete launch suite, and exact local artifact validation passed. |
| LAUNCH-047 | P2 | Accepted architecture boundary | The protected `main` branch blocks force-pushes/deletion and requires linear history, but does not require a pull request, hosted status checks, or signed commits before update. Scheduled publication currently originates on the trusted Mac because the source blocks datacenter ingestion, then GitHub validates before production deployment. | Preserve the automated publisher and fail-closed production gate for launch. Migrating publication to a dedicated signed GitHub App or PR-based updater is a separate credential/operations project; enabling incompatible branch requirements now would stop scheduled freshness. Direct write authority remains limited to the sole repository owner, default workflow tokens are read-only, and production deploys only a newly tested exact SHA. | Repository rules, collaborators, workflow permissions, secrets, Actions policy, pre-push gate, deployment currency checks, and rollback authorization were audited. This is an explicit source-branch trust boundary, not a claim that an administrator compromise can be prevented by repository code. |
| LAUNCH-048 | P3 | Resolved | Pre-launch audit found the rollback workflow's `Verify schema-neutral artifact attestation` step expanded the operator-supplied `release_sha` dispatch input directly into its `run:` command line, so the shell received attacker-influenced text before quoting. The exploit path was closed only by an earlier step's `^[0-9a-f]{40}$` check, leaving a template-expansion injection latent behind step ordering. | Bind the input to an `EXPECTED_REVISION` environment variable and reference `"$EXPECTED_REVISION"` in the command, matching the pattern the neighbouring `Verify exact rollback is live` step already uses. No other workflow interpolates untrusted text into a `run:` block; the three remaining expansions are GitHub-controlled `github.sha`. | Workflow-wide `run:`-block interpolation scan reports only `github.sha`; all three workflow files parse as valid YAML; the 380-test suite, Ruff, mypy, `py_compile`, shell syntax, plist lint, tracked-data validation, and the full build/inline-script/exact-release gate all passed after the change. |
| LAUNCH-049 | P1 | Resolved | Pre-launch audit found a stored markup-injection in the generated crawler stubs. `render_article_stub` embedded a registry article's source URL into its inline `<script>` with `json.dumps`, which escapes quotes and backslashes but not `<`, `>`, or `&`. `data_contract._https_url` accepted any path characters, so a registry URL such as `https://host/p/x</script><img src=x onerror=alert(1)>` passed validation, closed the script element early, destroyed the intended `location.replace` redirect, invalidated the stub's own CSP script hash, and rendered attacker markup on the site origin at `/a/<slug>.html`. Executed proof reproduced the break-out; the 88 Patreon and 11 FxEmpire registry URLs are re-fetched from remote sources each refresh, so the ingestion path is live. No tracked URL currently carries the payload, so the defect was latent, not exploited. | Added `_script_literal`, which escapes `&`, `<`, and `>` to the JavaScript string escapes `\u0026`, `\u003c`, and `\u003e` after `json.dumps`, mirroring `build_site.json_for_script`, and used it for the stub redirect so the CSP hash still covers the exact emitted text. Independently tightened `_https_url` to reject unencoded spaces, quotes, backticks, backslashes, braces, pipes, carets, angle brackets, and non-printable characters, so a hostile source URL now fails the refresh at validation instead of reaching any renderer. | New `test_registry_source_url_cannot_terminate_the_stub_script` proves the element closes exactly once, no `<img` survives, the literal still round-trips to the exact original URL, and the declared `sha256-` hash matches the emitted script; it was confirmed to FAIL against the pre-fix serializer. New `test_source_urls_must_percent_encode_markup_and_control_characters` covers both contract directions. All 726 tracked article and alternate URLs still validate, the rebuilt release is byte-for-byte identical to the pre-fix artifact, and the 382-test suite, Ruff, mypy, `py_compile`, shell syntax, plist lint, tracked-data validation, inline-script compilation, and exact-release validation all passed. |
| LAUNCH-050 | P3 | Resolved | The LAUNCH-049 fix was covered by a single hand-written payload, and `validate_release.py` cannot detect a renderer defect because it compares emitted bytes against a fresh call to the same renderer. A parser-based adversarial sweep over every stub-reaching string field found that the original defect was reachable through five distinct payloads, not one: `</script><img ...>`, `"><script>`, `</SCRIPT ><b>`, `<!--<script>`, and `]]><script>`. | Added a permanent sweep that renders 60 hostile field/payload combinations and asserts, with `html.parser`, that each value either fails closed during validation or renders as inert text: no element node outside the stub template's own tag set, no inline `on*` handler, exactly one balanced script element, and a CSP `sha256-` hash still covering the emitted script. | The sweep passes against current code and was confirmed to FAIL on five subtests against the pre-fix serializer, so it is a genuine regression guard rather than a tautology. Sitemap rows, `robots.txt`, `site.webmanifest`, `favicon.svg`, and the six data endpoints were separately confirmed to be literal-derived or escaped. Full 383-test suite, Ruff, mypy, and whitespace checks passed. |
| LAUNCH-051 | P2 | Resolved | Desk-workflow review of the built terminal found two defects. The command bar set `flex-wrap:wrap`, and because flex wrapping takes precedence over shrinking, `Export CSV` and `Inspector` dropped onto a second row at every desktop width in both themes (bar height 89px instead of 50px, and 129px in the dark theme at 1280px because its mono face is wider), costing a full row of grid height and making the layout theme-dependent. Separately, every keyboard action except search required a three-key `Alt+Shift` chord, so moving one row down a 1,234-row grid took `Alt+Shift+J`, and there was no way to jump to a named dossier without filtering. | Hold the command bar to a single row on the desk and restore wrapping only below the existing 1020px breakpoint, letting the result summary ellipsize instead of pushing controls down. Add unmodified `J`/`K` row movement, `/` search focus, and `?` reference, all placed after the existing editable/interactive guard so they cannot swallow typed input, with the `Alt+Shift` chords retained for existing muscle memory. Add a `Ctrl`/`Cmd`+`K` command palette over views, all 475 dossiers, and terminal actions; each entry delegates to the control that already owns the behaviour so the palette cannot drift from the visible UI. | Browser-driven checks confirm the bar is a single 50px row at 1728/1512/1280/1100px in both themes with no horizontal overflow, and still wraps without overflow at 900/768/430/390px where the palette also opens and fits. Palette checks confirm open, filter, run, dossier lookup, and Escape; bare `J`/`K` move and restore selection; and typing `jk test` into the search field is not hijacked. Zero console errors across all four views and both themes. Three new build tests cover palette ARIA/delegation, bare-key guard ordering, and the wrap contract; the 386-test suite, Ruff, mypy, inline-script compilation, and exact-release validation all passed. |
| LAUNCH-052 | P1 | Resolved | Independent watchdog runs from `30972395605` onward failed while rebuilding the trusted release: Substack began permanently redirecting `navnoorbawa.substack.com` API calls to the owned custom domain `www.navnoorbawaresearch.com`, and `fetch_json` rejected that final host as an untrusted origin. Scheduled Mac refreshes therefore stopped publishing; the last healthy snapshot remained `2026-08-04T07:32:23Z`, and by 2026-08-05 the 16-hour freshness gate correctly went red. | Trust both the publication subdomain and `www.navnoorbawaresearch.com` for Substack API responses while keeping stored article URLs on `navnoorbawa.substack.com`; recognize the custom domain in terminal source-URL routing; bind Medium RSS current-excerpt revision timestamps to the publication instant so degraded GraphQL fallbacks remain wire-valid; refresh the tracked catalogue to 489 articles / 1237 observations checked `2026-08-06T12:16:36Z`. | Focused Substack redirect tests accept the owned custom domain and reject foreign hosts; Medium RSS/snapshot regressions expect publication-bound timestamps; the 388-test suite, tracked-data validation, deterministic build, inline-script compilation, and exact-release validation passed for the refreshed snapshot. |
| LAUNCH-053 | P1 | Resolved | Main pushes `31102542355`–`31107404029` passed quality, but `actions/deploy-pages` repeatedly stalled in `deployment_queued` (`actions/deploy-pages#406`). Despite Action timeouts, production eventually began serving exact release `8c71e1cb3ede97dffcab5bf72a2ebc05b6520a9d`. A stale GitHub Pages service lock on `da4166db2ceba1a9094c1d2e2c361a7e58e91c80` then blocked newer releases even after the official action reported that it had cancelled the timed-out deployment. | Preserve serialized, fail-closed production ownership and exact-live checks; do not weaken artifact or freshness validation to conceal a GitHub platform queue incident. The external lock cleared, so no owner cancellation remains outstanding. The recurrence and false-retry gaps exposed by this incident are tracked separately rather than leaving this incident falsely open. | Exact deployment `31238434655` published current-at-audit release `8db31bc81535985235956502c7d6b888c451eddc` on its first attempt; independent watchdog `31241018251` passed exact revision, asset fingerprints, and freshness. Eighteen consecutive runs after recovery were green, and the Pages deployment API showed no waiting, queued, or in-progress deployment. Historical red runs remain retained as incident evidence. |
| LAUNCH-054 | P2 | Resolved | CodeQL still reported two high-security-severity `py/bad-tag-filter` alerts (`#86` and `#87`) in the adversarial share-stub tests. The production renderer was not affected, but both tests extracted `<script>` text with a case-sensitive regular expression that CodeQL correctly rejects as an unsafe HTML-tag filter. | Replace both regex extractions with the existing `extract_inline_scripts` structural `HTMLParser` helper, require exactly one inline script and an empty executable type, and retain the exact CSP-hash and hostile-payload assertions. No alert was dismissed or hidden. | Focused hostile-stub tests and Ruff passed locally. Hosted CodeQL run `31248875030` completed both Actions and Python analyses successfully for resolving commit `139a6b59e9a2b695f233e7310c464d8c0ceb6bae`; alerts `#86` and `#87` auto-closed and the open code-scanning alert count is zero. |
| LAUNCH-055 | P1 | Resolved | The Aug 6 Pages incident disproved the workflow's three-attempt recovery assumption: `deploy-pages` already issued a successful cancellation request after its ten-minute timeout, immediate retries remained cancelled, and a later SHA was blocked by the stale service lock. Each retry also occurred without a fresh `main` ownership check, so a SHA superseded during the first ten-minute wait could be deployed by a later attempt. Jobs API metadata further reports a failed `continue-on-error` step as successful, so rollback authority cannot infer the real outcome from that step alone. | Use one immutable Pages attempt only. Whether the action succeeds or times out, run a longer bounded exact-revision/fingerprint smoke against the configured Pages base URL; then freshly prove remote `main` and reconcile the action's real `outcome` with the smoke result. A late backend completion is accepted only when exact bytes are live and the SHA still owns `main`; a superseded run skips the required reconciliation certificate. Roll this out in two safe commits: first publish and prove the new deployment certificate while retaining compatibility with known-good rollback artifacts, then require ordered deploy, exact-smoke, post-deploy-authority, and reconciliation evidence after a green bootstrap source exists. | Phase-one run `31248875206` proved the new certificate for exact release `139a6b59e9a2b695f233e7310c464d8c0ceb6bae`. Strict-verifier release `31249321590` then passed 395 tests, immutable artifact validation, single Pages deployment, exact-live smoke, post-deploy authority, and reconciliation for `cda0adfcb8ce7fe607b3d5aba1b7231ccdf8c6d0`; all six commit checks have zero annotations. The verifier accepts both real hosted responses and rejects missing, skipped, failed, duplicated, or out-of-order evidence. Obsolete draft PR `#5` was closed as superseded. |
| LAUNCH-056 | P2 | Resolved | `automation_status.sh` could exit 0 while the LaunchAgent was actively refreshing, while the latest deployment was queued/in progress, or when the latest green run belonged to an older `main`; its success message therefore did not always mean the current release was settled. The emergency rollback job also allowed 15 minutes even though `deploy-pages` alone can use 10 minutes and the exact-byte smoke can use another 230 seconds, leaving only 70 seconds for checkout, authority, artifact, attestation, upload, and diagnostics. | Treat active ingestion and unsettled deployment as explicit pending/nonzero states with wait-and-rerun guidance; require the latest successful run's exact `headSha` to equal remote `main`; retain distinct repair guidance for actual failures. Make the rollback action's 600,000 ms ceiling explicit, raise the job ceiling to 25 minutes, and pin a five-minute setup/network margin in regression arithmetic. | Fake-launchctl/GitHub regressions cover idle-current success, active refresh, both deployment-pending states, stale successful SHA, failed exit, unavailable remote state, and missing or malformed run evidence. Hosted release `31248875206` passed the 393-test quality gate, immutable rollback build, exact production smoke, authority proof, and reconciliation for current `main`; local status reports that same settled release healthy. |
| LAUNCH-057 | P1 | Resolved | The terminal conflated publication access with captured-body coverage and offered no source-aware continuation path when a reader reached a paid Substack boundary. A generic promotion would also misrepresent locked Medium or paid Patreon articles, while a client-side paywall on public GitHub Pages would expose every supposedly hidden byte. | Add wire-schema-v3 `publication_access`; derive it from exact source audience; separate access from indexed coverage in filters, badges, exports, and method copy; show an article-derived conversion panel only for canonical paid Substack records; distinguish an exact anonymous preview from metadata-only state; rank related premium notes deterministically; send no terminal context in outbound URLs; document the editorial workflow; and fail the public contract for a member row marked `full`, a preview over 1,200 characters, a false length/hash, an unsupported source-access enum, or a brief/observation not bound to that exact preview. | Release `56b76fcf014c1ae69aca5ffa1026f3c200da6f08` contains 493 articles and 101 observations. Its 330 member-source rows consist of 64 exact anonymous Substack previews (14,500 characters total; 642 maximum) and 266 metadata-only rows; five observations are digest-bound to those previews. The 415-test suite, tracked-data validation, Python syntax, Ruff, mypy, shell/plist checks, deterministic build, inline-JavaScript compilation, exact artifact validation, and light/dark desktop/mobile browser QA passed. Hosted deployment `31254802830` passed every quality, artifact, authority, deploy, reconciliation, and exact-live-smoke step; an independent exact-fingerprint smoke also passed on attempt one. |
| LAUNCH-058 | P0 | BLOCKER | Public commit `001d8e8` retains 663 member-source brief spans (212,655 characters) and 1,131 member-derived observations (425,103 passage characters) created under the earlier cache policy. Previously published Pages bytes and retained 90-day rollback artifacts may contain the same material. The sanitized working snapshot is designed to stop propagation in future releases, but a normal commit/deploy cannot remove Git object history, artifacts, forks, clones, CDN/search caches, or other copies. | Do not rewrite history or delete audit/release evidence under the current repository rules. The owner must first decide whether historical publication is authorized. If removal is required, explicitly authorize and coordinate a destructive all-ref Git purge, artifact/release deletion, Pages/cache replacement, search-cache removal requests, collaborator notice, and fresh-clone verification. Record the approved scope and preservation plan before acting; no process can guarantee recall of third-party copies already made. | Pending explicit owner authorization and an externally coordinated purge decision. Current-snapshot validation can prove that new release bytes contain only exact anonymous previews or metadata, but it cannot close the historical-exposure boundary. |
| LAUNCH-059 | P1 | Resolved | CodeQL run `31254802548` opened high-severity path-flow alerts `#88` and `#89` because the previous-snapshot regression helper performed separate `Path.exists()` checks on two operator-supplied paths before loading them. Besides the alerts, a missing explicit baseline silently disabled part of the regression gate and the check/read split created a local time-of-check/time-of-use gap. | Load each supplied previous snapshot exactly once at the CLI boundary, pass parsed lists into filesystem-free regression validators, treat only an omitted option as “no baseline,” and fail closed when an explicitly supplied file is missing or unreadable. Remove the identical article-baseline existence probe proactively. | Release `9c80cd38b606fec5b2e8f4449e49ef76d9345446` passed the 416-test suite locally and in hosted deployment `31255368872`; every static-analysis, quality, authority, deploy, reconciliation, and exact-live-smoke step succeeded. CodeQL run `31255368747` auto-closed alerts `#88` and `#89`; all six commit checks report success with zero annotations and the open code-scanning alert count is zero. An independent exact-fingerprint production smoke passed on attempt one. |
| LAUNCH-060 | P1 | Resolved | The first Structuring Desk prototype treated 101 passage rows from only 31 source notes as independent evidence; short tokens such as `FX` and `AI` could normalize to an empty query and return the full corpus; multiword questions used OR matching; one universal U.S.-rates curve was attached to unrelated markets; ordinary selection and research state could be written into hidden URL history; and a decision packet could inherit an arbitrary top-ranked passage without the analyst choosing it. | Start with no evidence set until the analyst provides explicit scope; cluster and count by source note while retaining passage counts; preserve exact-underlying, authored-title, structured-field, and loose-text provenance as separate retrieval tiers; require all material query tokens; make macro context optional and bind every observation to an official observation date on or before the article's publication calendar date; keep ordinary Structure state in page memory and serialize it only through **Copy view**; and require the analyst to anchor one exact displayed passage before packet creation. Retain explicit boundaries against invented holdings, prices, P&L, sizing, exposure, liquidity, compliance conclusions, or recommendations. | The 456-test suite, tracked-data validation, deterministic 1,014-file build, two-script compilation, exact release validation, Python/static/shell/plist checks, frozen-tree stability proof, and light/dark desktop/mobile browser QA passed. Hosted run `31434225307` then passed every quality, artifact, rollback, authority, Pages deployment, exact-live-smoke, and reconciliation step for release `012126b7f326a3783c5f0b24c55c484e9e992ac7`. |
| LAUNCH-061 | P2 | Resolved | The scheduled refresh completed its first full suite and successfully published `6e6c9f8`, but a later refresh failed after the exact release validator's CLI regression exceeded its fixed 60-second child-process timeout under heavy local contention. The validator reported no contract error; the transaction correctly restored the prior snapshot, but launchd retained exit code 1 and emitted a false operational failure. | Preserve the child-process fail-closed boundary while raising only this exact release proof to a named 180-second ceiling. The validator's assertions, release fingerprints, outer transaction timeout, rollback behavior, and hosted quality gate remain unchanged. | The exact CLI proof passed in 27.4 seconds and the complete 456-test frozen-tree suite passed with the new bounded ceiling. Commit `7f495b9` passed the exact local release gate, and the scheduled LaunchAgent completed that pushed tree with exit code 0, confirming the bounded timeout no longer produces a false scheduler failure. |
| LAUNCH-062 | P1 | Resolved | Final provenance review found that the Desk called a daily Treasury reading the official close “at or before article publication.” The lookup is keyed to the article's calendar date, not its timestamp; 24 of 31 observation-bearing source notes receive a same-calendar-day reading, which can post-date an intraday article and therefore cannot prove pre-publication market conditions. | Describe the field as an official observation dated on or before the publication calendar date everywhere it appears—in the evidence row, coverage panel, copied memo, diligence packet, documentation, and tests. State explicitly that daily observations are not timestamp-aligned and may post-date a same-day article; retain the event/entry/position-date and causality boundaries. | Focused UI/memo/packet/export/timing regressions, independent security review, the complete 456-test suite, exact release validation, and frozen-tree static checks passed. Hosted run `31434225307` published and verified the corrected wording and exact evidence artifact. |
| LAUNCH-063 | P1 | Resolved | Hosted CodeQL completed successfully for `012126b` but opened critical `py/partial-ssrf` alert `#91` because CLI `--years` flowed into the fixed Treasury query string, and high-severity `py/path-injection` alert `#90` because CLI `--merge` flowed into the dataset reader. Integer parsing made query injection infeasible, and this is a local scheduler rather than a server, but the interface still exposed unnecessary network, arbitrary-read, and arbitrary-write controls. | Remove every Treasury fetch CLI control. Derive the UTC current/prior years internally; read only the script-root tracked dataset; emit validated compact JSON only to stdout for `refresh.sh` to redirect into its private transaction directory; pin one exact official HTTPS feed; disable environment proxies; reject redirects before a second request; and verify the final response URL before reading. Legacy arguments now fail before filesystem or network access, and the byte-exact tracked fallback remains fail-closed. | Fifteen focused fetch/transaction tests and an independent 28-test path-security review passed. The complete frozen-tree gate then passed 464 tests, tracked-data validation, the exact 1,014-file release, Python/static/shell/plist checks, and immutability proof. A real no-write official-feed round trip returned direct HTTP 200 with 401 validated trading days through 2026-08-10. Commit `7f495b9` passed the exact local release gate. Hosted CodeQL run `31438269160` completed successfully, auto-closed alerts `#90` and `#91`, and left zero open code-scanning alerts; Validate and Deploy Pages run `31438269748` passed the quality gate, immutable artifact validation, Pages deployment, exact-live smoke, post-deploy authority, and reconciliation for the same commit. |
| LAUNCH-064 | P1 | Resolved | At 1280×720 the Structure Desk's first evidence began at y=659, below the initial viewport, while coverage cards and readiness-style scores led the page. The same field served as both the analyst's decision question and retrieval query; title matches could make unrelated passages look eligible; optional Treasury bands could affect the retrieved set; **Copy view** wrote share state into normal URL history; and the local workflow solicited confidence, payoff, implementation, and portfolio fields that invite sensitive or falsely precise entries. | Rebuild the view as an evidence-first **Research Evidence Desk** with separate memory-first analyst framing and deterministic literal all-term evidence-subject retrieval; lazy-load observations only after scope exists; cluster primary evidence by source note; separate headline, structured-field, and text-only context from passage-level eligibility; require one exact displayed passage anchor; label parser outputs as candidates or detected phrases rather than conclusions; replace coverage/readiness scores with raw counts and human research gaps; make optional Treasury data post-retrieval provenance that cannot alter membership or order; generate **Copy view** URLs without mutating active history; reduce the tab-local scratchpad to non-confidential human Research Task fields; and place source-specific premium continuation only inside an expanded paid Substack note. Bind every task surface to its retained source snapshot, label current-release drift separately, reject unsafe/missing snapshots and unsafe imported IDs, canonicalize legacy storage without reinterpreting old fields or attestations, make all 250 bounded current-missing tasks first-class, preserve capture-time review flags with explicit unavailable provenance, require separate approval for same-ID retained-source import conflicts, and disclose active-release snapshots created from legacy ID-only bookmarks. | The frozen worktree passed all 474 tests, tracked-data validation for 500 articles and 101 observations, inline-script compilation, the exact 1,014-file release validator, Python syntax, Ruff, mypy across 26 production modules, all shell and plist checks, and an immutability proof. Browser QA passed in FT-style light and Bloomberg-style dark themes at desktop and 390×844: evidence begins inside the first viewport, no horizontal overflow occurred, every visible mobile control measured at least 44 pixels tall, and the console remained clean. Commit `ea6c6a6` then passed hosted CodeQL run `31454675122` and Validate and Deploy Pages run `31454675439`: CI reran all 474 tests; every required job completed successfully with zero annotations; deployment `5843735887` reached success; all open CodeQL, Dependabot, and secret-scanning alert counts were zero; the retained rollback artifact independently revalidated; and an independent exact-byte production smoke passed on the first attempt. |
| LAUNCH-065 | P1 | In progress | A fresh build placed 296,249 bytes of compact article rows inside an 846,246-byte `index.html`, leaving only 53,754 bytes below the fixed 900,000-byte first-load ceiling. Routine archive growth would therefore block a valid deployment even though the underlying catalogue remained sound. | Move the compact rows to same-origin `article_catalog.json`; bind its exact bytes into the tested HTML with SHA-256 and snapshot checksum; verify HTTP, digest, exact envelope, wire version, count, and unique identities before starting the application; expose an accessible fail-closed retry/status screen; and retain the existing HTML and aggregate release budgets. | Focused real-build, release-validator, live-smoke, no-JavaScript, stale-shell, keyboard-focus, and executed Node failure/retry regressions pass. A synthetic 140-article expansion grows the catalogue by more than 80 KB while keeping HTML below 900 KB and below half that growth. The frozen combined tree passed all 494 tests, exact tracked-data validation, inline-script compilation, the exact 1,055-file release validator, browser recovery/mobile/theme checks, and every Python/static/shell/plist check. Hosted exact-release verification is pending. |
| LAUNCH-066 | P1 | In progress | Operator status and the hosted watcher could remain green indefinitely while the manifest explicitly reported Substack and Medium in validated degraded fallback modes; freshness timestamps alone did not distinguish current primary-source ingestion from a repeatedly republished cache. The first implementation also accepted a CLI manifest path like the earlier CodeQL path-flow incident and allowed a tracked manifest to remove both streak fields and reset itself to a legacy first observation. | Persist `degraded_since` and `consecutive_degraded_checks` across refreshes; permit validated fallback publication with a warning; make local status nonzero for any degraded source; warn the independent watcher transiently and fail it after 48 continuous hours; require a successful watchdog for exact current `main`; consume the manifest only through stdin; and reject removal, contradiction, rollback, or discontinuity in streak state. | Fifty-three focused source-health, manifest, transaction, deployment, shell, Python, Ruff, and diff checks pass. Real local status now reports the currently degraded Substack and Medium adapters instead of false health. The frozen combined tree passed all 494 tests and every exact local release gate. Hosted watcher and persisted non-legacy refresh verification are pending. |
| LAUNCH-067 | P1 | In progress | The checkpoint parser accepted only years beginning with `20`, silently losing otherwise valid 2100+ authored checkpoints, compact UI dates displayed only two year digits, and Treasury date validation accepted impossible calendar keys such as `2026-02-31`. | Accept strict four-digit checkpoint years and let Gregorian construction validate the date; validate Treasury keys and feed dates with exact `date.fromisoformat` round trips; reject malformed upstream dates; and render compact dates with all four year digits. | Focused 2100/non-leap/2400-leap/feed/date-display regressions and the frozen 494-test combined release gate pass, together with exact artifact, Python, Ruff, and mypy verification. Hosted exact-release verification is pending. |
| LAUNCH-068 | P1 | Open | The six public data endpoints currently occupy 2,131,069 of the 4,000,000-byte bundle ceiling; `search_index.json` occupies 227,134 of 500,000 bytes; `related.json` is 1,119,349 bytes and its builder remains quadratic. Straight-line size ratios reach reviewed limits at roughly 976–1,145 catalogue records, so moving only the bootstrap catalogue postpones rather than removes archive growth failure. | Design a versioned, deterministic year or stable-prefix shard manifest; keep a bounded hot index; lazily fetch historical search/related partitions; preserve old readers during migration; and add 10×/100× growth, exact-shard, missing-partition, and rollback compatibility tests. Do not merely raise the ceilings. | Pending implementation and a schema-compatibility migration review. |
| LAUNCH-069 | P1 | Open | Patreon ingestion has lifetime limits of ten pages and 500 posts. Once the publication crosses the cap, refresh fails and retains the previous registry forever even if the API remains healthy. | Replace lifetime caps with bounded incremental cursor ingestion that stops after verified overlap with the prior catalogue, plus periodic complete reconciliation and explicit byte/time/page/cursor-loop guards. | Pending adapter implementation, long-catalogue fixtures, and a two-run overlap/reconciliation proof. |
| LAUNCH-070 | P1 | Open | Runtime article/observation IDs are 56-bit hashes derived partly from mutable URLs or descriptions, while human slugs are a global identity namespace and Unicode physical filenames. Publisher slug/domain edits, text corrections, cross-source slug reuse, case-folding, or normalization differences can orphan local tasks and make assets non-portable over decades. | Introduce versioned 128-bit IDs derived from source plus immutable source-native identity, dual-read the old IDs with a checked migration map, and use an ASCII source-qualified physical asset key while preserving current routes as redirect aliases. | Pending identity-version design, collision corpus, cross-filesystem tests, and local-storage migration proof. |
| LAUNCH-071 | P0 | BLOCKER | The repository has one personal owner/admin, ingestion runs on one logged-in Mac, production has no owner-controlled custom domain or secondary host, and the GitHub-hosted watcher shares the deployment failure domain. Owner lockout, incapacity, Mac loss, account suspension, or provider loss can end publication and break every canonical link. GitHub's published Pages limits also say the service is not intended for a site primarily facilitating commercial transactions, impose a 100 GB/month soft bandwidth limit, and permit rate limiting, while this archive deliberately leads with a paid-research subscription path. | The owner must choose and authorize an organization/custodian model, at least two hardware-key administrators, a named successor, a narrowly scoped publisher identity, an auto-renewed owner-controlled domain, a commercial second static host, and an independent regional dead-man monitor. Document and rehearse annual access, DNS, failover, and succession recovery. | Pending explicit owner decisions, external account/domain work, and witnessed recovery drills; repository code cannot create or custody these authorities safely. |
| LAUNCH-072 | P0 | BLOCKER | Ignored continuity caches exist only on the scheduled Mac, and exact rollback artifacts expire after 90 days. Git history is not durable custody of authenticated/private continuity state or the exact attested built site, and ordinary repository changes cannot provide independent encrypted preservation. | The owner must authorize encrypted 3-2-1 backup for private caches/configuration/keys and immutable off-provider, content-addressed release packs containing the site, attestation, exact Git bundle, checksums, and decoder/toolchain metadata. Define retention, access, deletion, succession, and quarterly clean-machine restore drills before any external copy is made. | Pending storage/provider/privacy decisions and an externally witnessed restore exercise. |
| LAUNCH-073 | P1 | Open | Durability still depends on a bypassable local pre-push hook, direct writes to `main`, an EOL system Python fallback, floating runner/Node versions, local-vs-CI tool drift, unbounded launchd logs, one broad long-lived credential, and rescue workflows stored only on default `main`. These controls work today but do not constitute a reproducible or independently recoverable operating environment. | Move publication through a narrowly scoped candidate branch with hosted required checks and a controlled merge path; pin/archive a reproducible toolchain and runner recipe; add disk/tool/auth/connectivity preflight plus log rotation; maintain a cold standby with a single-writer lease; sign/export releases; and keep a rehearsed rescue path outside default `main`. | Pending staged operations design; changes to branch authority, credentials, runner custody, and rescue hosting require owner review. |
| LAUNCH-074 | P2 | Open | Treasury schema validation hard-rejects any future legitimate yield outside `[-5, 25]`. This fails closed rather than corrupting data, but a structurally valid regime change could freeze the official series indefinitely. | Keep finite-number validation in the durable schema, but move economic plausibility ranges into a versioned quarantine/alert policy that requires explicit operator review before widening. | Pending schema-policy separation, boundary fixtures, and a documented operator override audit trail. |
| LAUNCH-075 | P3 | In progress | The optional Markdown export helper hard-coded one developer's absolute Downloads paths, so it failed after repository relocation or succession to another machine. | Resolve its input, ignored corpus cache, and output relative to the script directory. | Static portability scan, the frozen 494-test suite, Python compilation, Ruff, and mypy pass. Hosted exact-release verification is pending. |
| LAUNCH-076 | P3 | Resolved | The deferred-catalogue startup wrapper routed any exception raised after catalogue verification through the same catalogue-failure handler. A future initialization error after listeners or state mutations began could therefore be mislabeled and make an in-page Retry repeat partial side effects. No current release path triggered this failure, and the catalogue failure itself occurred before application side effects. | Track the exact transition from side-effect-free catalogue acquisition into application initialization. Keep in-page Retry only before that boundary; after it, hide and disable Retry, label the failure as initialization rather than catalogue availability, and expose only a cache-busted clean-document reload. | Executed runtime injection proves one partial initialization cannot expose the re-entrant Retry and instead requires the clean reload. The 553-test exact-commit release gates, deployment run `32731807275`, CodeQL run `32731806235`, and independent watchdog `32732629037` passed for `8bd108120a9c615dfa165dfba5f8328937c1f280`; live browser startup completed on that exact revision with the shell uninert and no console errors. |
| LAUNCH-077 | P1 | Resolved | Scheduled refreshes rolled back valid publication candidates because a build regression test hard-coded the prior Crypto facet cardinality, a related-graph test hard-coded legacy members of five live top-five results, and a synthetic growth build allowed only 90 seconds on the supported Apple Python 3.9 runtime. The LaunchAgent then waited for the next calendar slot after each nonzero exit, so repeated local gate failures left the published snapshot 36.2 hours old and caused **Monitor Published Research** run `31937227754` to fail its freshness gate even though exact-live and release-integrity checks still passed. The old overlap path also returned success for any live PID in the lock, which could stop scheduled recovery or let PID reuse block publication indefinitely. | Recompute the complete expected underlying-facet table from the candidate corpus; test related ranking against a fixed curated fixture while retaining corpus-wide truthfulness invariants; give only the synthetic growth subprocess a bounded 240-second ceiling; and run each calendar event through a foreground supervisor capped at three attempts with 15-minute gaps. Scheduled lock contention returns temporary-failure code 75 while manual overlap remains a clean no-op. Authenticate lock ownership with PID, process start, command, physical repository root, and working directory; clear stale/foreign ownership; preserve the final nonzero exit after retry exhaustion; and do not use unbounded launchd `KeepAlive` retries. | The reinstalled LaunchAgent subsequently published five consecutive refresh commits through `74f86ca` with final exit 0 and a clean worktree. Validate and Deploy Pages run `32586548593` and exact-current watchdog run `32589684119` both passed for that revision; automation status reports the updater idle, the current deployment successful, and every publication source healthy. |
| LAUNCH-078 | P1 | Resolved | The schema-neutral rollback bundle's strict live-byte smoke relied only on the active Python installation's platform CA path. The scheduled Mac's default Python 3.12 installation had no usable CA file, so an emergency/manual rollback rehearsal failed with `CERTIFICATE_VERIFY_FAILED` even though normal production smoke and the retained bundle itself were valid. | Build one verified SSL context per rollback smoke, use `certifi` when installed and the platform trust store otherwise, inject it into the HTTPS opener, and reuse it across bounded concurrent requests while preserving hostname verification, certificate verification, same-origin redirect enforcement, and exact-byte validation. | Focused rollback tests prove the certifi path, platform fallback, opener wiring, single-context reuse, redirects, retries, and exact-byte rejection. The same code passed real HTTPS smoke under the affected Python and every subsequent release gate; run `32586548593` retained verified bundle `9479230140` for exact revision `74f86ca` through 2026-11-20 and revalidated it before deployment. |
| LAUNCH-079 | P1 | Resolved | Source health conflated publication discovery with permanently limited article-body coverage. Every expected paid Substack excerpt forced the whole source into `degraded`, while Medium's retired private GraphQL archive returned 403 and any official-RSS fallback was always degraded. Medium's old fallback could also trust a no-overlap merge on the next run, and a schema-valid but stale legacy archive could outrank a newer supported RSS head. Substack additionally exposed compatible short and long list previews for three live public rows; choosing the first mismatch made their cached excerpts oscillate and could keep the source degraded forever. | Keep expected member/stable excerpt limitations as article-level notices. A changed public list surface bypasses stale cache, publishes the longest deterministic compatible current excerpt, and opens one degraded observation; an identical later surface is stable coverage, while mutually inconsistent live surfaces remain degraded without changing bytes. Require two identical complete Medium RSS windows, exact timestamps and contiguous newest-history overlap; quarantine any unproved gap without letting it seed the next run. Bind every purported complete legacy archive to that stable RSS edge. Permit only one exact, three-day, operator-reviewed direct-profile bridge for the reviewed 10+2 ID sequence, and persist its exact provenance in the snapshot manifest. | Focused and live multi-pass proofs passed, then multiple real transactional refreshes published healthy snapshots through `74f86ca`. The current manifest reports all four publication sources `ok`; deployment `32586548593` and watchdog `32589684119` independently passed source-health, freshness, rebuild, and exact-live verification. |
| LAUNCH-080 | P1 | Resolved | Hosted CodeQL run `32384374475` opened high-severity `py/path-injection` alert `#92` at `fetch_medium_posts.py:39`: the `PREVIOUS_MEDIUM` environment value flowed into the prior-catalogue `open`, and the same adapter unnecessarily exposed environment controls for its candidate and status paths. The alert API was unavailable to the current token (HTTP 403), but the reported location and source-to-sink code path were directly verified. | Remove all three Medium filesystem-path environment controls. Bind the prior catalogue and reviewed bridge to the resolved script root; bind candidate and status output to fixed filenames beneath the process working directory; run the zero-argument absolute collector from `refresh.sh`'s private transaction directory; and reject command-line arguments before any filesystem or network work. | The focused and full release gates passed, hosted CodeQL auto-closed alert `#92`, and every later exact-revision Python/Actions scan stayed green. The owner-authorized APIs report zero open CodeQL, Dependabot, and secret-scanning alerts; current deployment and watchdog runs `32586548593` / `32589684119` passed for `74f86ca`. |
| LAUNCH-081 | P2 | Resolved | Public copy and metadata still branded the product as an institutional “Navnoor Research Terminal,” used investment-committee and source-backed claims, and mixed navigation names such as Evidence Desk, Latest Brief, Evidence Monitor, Research Library, and Research Tasks with ambiguous dossier/brief terminology. The tracked social image and visible N/R marks repeated that contract. | Rename the existing product **Navnoor Research Archive** (short name **Navnoor Archive**) with the descriptor “Published research index with source-linked passages and capture provenance.” Align visible navigation to Passage Search, Article Record, Parsed Passages, Article Index, and Local Review; use source-bounded passage terminology; keep internal routes, schemas, files, storage keys, and identifiers unchanged; replace the tracked 1200×630 social image and public NA marks; preserve publication snapshots and historical launch records. | The full suite, exact artifact validation, hosted deployment, and browser QA passed. Production now serves title `Navnoor Research Archive`, exact revision `74f86ca`, the five canonical navigation labels, coherent NA social metadata/cards, and the corrected GitHub About description/topics; publication schemas, routes, storage identifiers, and snapshots remained compatible. |
| LAUNCH-082 | P1 | Resolved | The first scheduled refresh after the archive release generated a valid candidate, then rolled it back because all 39 Node-backed client runtime tests failed at process bootstrap with `uv_cwd` / `EPERM`. The LaunchAgent does not inherit Terminal's macOS privacy grant for the repository beneath Downloads, and the test helper launched Node with that protected directory as its working directory even though every runtime harness is self-contained. | Resolve the repository path for reading source, but launch the self-contained Node evaluation from the fixed system temporary directory `/tmp`. Keep the scripts entirely in-memory so no repository-relative test behavior is weakened. | The targeted Node suite passed immediately from `/tmp`; the bounded supervisor then recovered and published the fix rather than exhausting retries. Five later scheduled refreshes, their full local/pre-push gates, current deployment `32586548593`, and exact-current watchdog `32589684119` all passed, while automation status reports final exit 0 and an idle clean publisher. |
| LAUNCH-083 | P1 | Resolved | The landing view hid the strongest research and review mechanics behind an empty query-builder state; light and dark modes used different product grammars; Structure could open hidden mobile drawers and inert the visible workspace; source coverage omitted Patreon and FX Empire and overstated non-registry records as captured text; Article Record could not hand off an exact passage directly; and first-pass source/freshness, baseline, and passage-cap labels could contradict the underlying release. | Add a release-bound principal brief, clock-aware freshness, exact captured-text versus metadata-only coverage for all four sources, reversible explicit review baselines, a strict source-specific outbound-link allowlist, and a complete exact-passage Article Record handoff. Unify theme geometry and typography, honor system preference before paint, restore accessible control boundaries/selected states, and remove hidden drawers from modal state in every route and keyboard path. | The final 544-test suite, focused deterministic build, inline-script, exact-release, palette/contrast, source-count, cross-source and hostile-URL, passage-completeness, executed review-storage rollback, 320-pixel mobile-header, overlay, print, and freshness contracts passed. Independent product, accessibility, and release-safety audits found no remaining material issue. |
| LAUNCH-084 | P1 | Resolved | The acquisition landing still presented an analyst workstation before the research product: five internal destinations, two inputs, visible refinement controls, dense operational telemetry, and many full-height cards preceded a clear promise or purchase path. The subscriber action was secondary, stale filters could redirect featured-note entry, Home could preserve the advanced workspace, and active-review counts could open archived items. | Rebuild the default route as a premium owner-first research home with one promise, one featured note plus three compact rows, one local search, three primary destinations, an explicit full-access CTA, a concise review agenda, and collapsed release coverage. Preserve all five internal routes and evidence controls behind progressive disclosure; reset owner entry state, restore focus through deferred loading, keep active-review and zero-new destinations exact, expose health state honestly, and retain privacy, provenance, source allowlists, and 44-pixel mobile targets. | The full 548-test suite, tracked-data validation, Python compilation, Ruff, mypy, shell/plist/diff checks, fresh temporary build, inline-script compilation, and exact release validation passed. Release `66299ab5d3a2528f43d92944b76f0b48576ea656` passed hosted deployment and exact-live smoke in run `32715887784`, CodeQL run `32715886931`, and independent freshness watchdog `32716237173`; automation status then reported current deployment, current watchdog, and all four publication sources healthy. |
| LAUNCH-085 | P1 | Resolved | Launch-eve browser and source audits found that the owner home still read as a generic cool-blue operations dashboard: the subscription action was secondary, queue telemetry occupied the hero, major titles used the system sans, the 320-pixel hero hid all product content below the first viewport, the mobile archive tab bar showed a shadow without overflow, and the product name collapsed to an ambiguous “NA.” The JavaScript bootstrap also left an empty card until the bottom script ran, several selected controls lost their state in forced-colors mode, data-health disclosure had no visible affordance, and discovery/social copy still described the old technical index. | Introduce one researched institutional editorial system—warm paper, midnight navy, ink, pale blue, and restrained brass—with role-bound semantic colors and preserved contrast gates. Lead with full research access, move release-bound buyer proof beside the promise, make archive search the secondary panel, disclose local review tools, use serif display typography, shorten the mobile hero, show a meaningful mobile wordmark, remove the false overflow shadow, render the verified-load copy on first paint, extend forced-colors selected-state outlines, add the data-health chevron, and align metadata, favicon, a cache-busted root social preview, and deterministic article-card branding and colors. | All 550 tests, tracked-data validation, Python compilation, Ruff, mypy, shell/plist/diff checks, a fresh temporary build, inline-script compilation, and exact 1,155-file release validation passed. UI commit `aa6e7c2b58eff2b6b007664b59855616efcd5357` passed hosted deployment and exact-live smoke in run `32723419932`, CodeQL run `32723419385`, and independent current-release watchdog `32723828030`; automation status then reported the updater idle, the current deployment and watchdog successful, and all four publication sources healthy. Production browser QA at 1280×720 and 320×740 in light and dark found zero horizontal overflow, preserved 400/564-pixel hero geometry across themes, 44-pixel primary and secondary actions, a visible short mobile wordmark, no false tab shadow, a correctly ordered subscriber boundary, the exact revision and fingerprinted social URL, and successful latest-article navigation. Independent UI, release-risk, and final test audits found no remaining material code issue. |
| LAUNCH-086 | P1 | Resolved | Any unknown or retired project route returned GitHub's stock “Page not found · GitHub Pages” document because the release had no `404.html`. The formerly advertised `/og.jpg` social URL also became a stock GitHub 404 after the fingerprinted image rename, even though historical article-slug review found no removed published stub. | Generate a self-contained, script-free, noindexed archive-owned recovery page; independently reproduce and byte-validate it; include it in the immutable support fingerprint; make hosted smoke request a release-unique nonexistent URL and require the exact branded bytes with an honest HTTP 404; and retain `og.jpg` as an exact 200 compatibility alias for the current fingerprinted image. Provider, account, and DNS failure remain the separate P0 boundary in LAUNCH-071/072. | The exact 1,157-file build, 553-test commit and pre-push gates, deployment `32731807275`, and watchdog `32732629037` passed for `8bd108120a9c615dfa165dfba5f8328937c1f280`. An independent live probe received HTTP 404 and `text/html` at release-unique route `__nrt_missing_e075e1b44c90b6cb.html`, with bytes exactly equal to committed `404.html` (`dd9c04088d0b6296b403d9f7dbbf54ae04aaaa7d6562826032bb99f14bed34b1`); `/og.jpg` returned HTTP 200 `image/jpeg` and exactly matched the fingerprinted image (`75f2b9d90433466de6431e4fe3f8321fb878922b9348bfff39cd33f1da4ec3a1`). Production browser QA at 1280×720 and 320×740 found no horizontal overflow, a 48-pixel home action, zero scripts, no GitHub copy, and successful return to the exact current release. |
| LAUNCH-087 | P1 | Resolved | Production route audit observed transient connection failures and one HTTP 503 while every affected asset recovered on a bounded retry, but browser release fetches made only one attempt and could therefore replace the whole first load with a blocking error. The stale-shell marker was never cleared after success and was keyed only to the data checksum, so two UI releases over identical data could reuse the same cache key and prevent recovery from the second mismatch; its visible Retry fetched only JSON rather than a fresh document. | Retry only network failures, required-asset 404, HTTP 408/409/425/429/5xx, and per-attempt timeouts up to three total attempts with short bounded backoff and cleanup; fail fast for explicit offline state and invalid-request/authorization responses; key stale-shell navigation to the exact site revision plus a per-navigation nonce; clear all internal recovery query parameters only after complete initialization; expose a separately cache-busted latest-release document reload; and move focus to that reload if initialization has begun. | Executed runtime tests prove reset→503→200 recovery, bounded Pages-404 propagation retry, immediate invalid-request rejection, exact attempt/controller/timer cleanup, safe timeout copy, same-data successive-release recovery, explicit document cache busting, focus transfer, and the non-reentrant initialization boundary. Exact deployment `32731807275`, CodeQL `32731806235`, and watchdog `32732629037` passed for `8bd108120a9c615dfa165dfba5f8328937c1f280`; a separate production smoke passed on its first attempt, and browser navigation from the live 404 initialized that exact revision with internal recovery state cleared, the application shell active, and zero console messages. |
| LAUNCH-088 | P1 | Open | GitHub Actions currently retains 114 unexpired artifacts consuming 289,108,298 bytes (275.7 MiB) after 26 days: 98 90-day rollback artifacts use 255.2 MB and 16 seven-day Pages artifacts use 33.9 MB. At the observed 3.77 successful releases per day and current roughly 2.66 MB rollback size, steady-state storage projects near 0.93 GB before other repositories or packages. The account plan, payment method, and hard-budget state could not be independently proven; exhausting an enforced limit can make artifact upload fail and block deployment. | Establish owner-visible account-level Actions storage and budget evidence plus a soft, non-blocking budget alert; add a repository artifact inventory and forecast check; retain short rolling per-commit bundles plus 90-day certified daily or weekly recovery points; and export long-lived immutable release packs off-provider under LAUNCH-072 rather than deleting the only recovery copy. | Acceptance requires synthetic inventory tests for threshold and projection math, automated warning before 75/90/100%, proof that the production artifact and declared rollback generations remain downloadable and attested after pruning, and a hosted release that uploads and deploys successfully with the retention policy active. |
| LAUNCH-089 | P1 | Open | The scheduled publisher writes its last-success marker and exits zero immediately after `git push`; hosted deployment may still be queued or may later fail. `automation_status.sh` is manual and accepts run-level `completed/success` plus matching `headSha` without authenticating the required deploy, exact-live smoke, post-deploy-authority, and reconciliation steps through the stricter verifier already used by rollback. Local “refresh succeeded” can therefore coexist with an uncertified GitHub release until an operator notices. | Keep ingestion success distinct from publication certification. After push, discover the exact-SHA workflow run, track pending or failure separately, and use `verify_workflow_run.py` plus exact live fingerprints before recording a certified deployment; alert without blindly repeating an ambiguous Pages attempt. Make `automation_status.sh` consume that exact evidence. | Fixtures must reject no run, queued run, stale SHA, missing/skipped/failed/duplicate/out-of-order deploy evidence, `continue-on-error` ambiguity, and live-byte drift; accept only the exact current certified run. A hosted drill must prove a safe workflow failure leaves prior production intact and records and alerts non-certification, then that a later green run records its exact run ID, SHA, and fingerprints. |
| LAUNCH-090 | P2 | Open | Emergency rollback smoke opens a new urllib opener and TLS/DNS path for every archived file; the current release requires 1,155 independent live fetches, while the schema permits 10,000 files and the rollback job remains capped at 25 minutes. A real one-attempt audit of current artifact `9519120891` passed extraction and attestation but failed two otherwise-valid files on transient DNS. Eight fixed-delay retries usually mask this, but catalogue growth, DNS churn, HTTP 429/503, or repeated handshakes can exhaust the emergency window. | Reuse bounded persistent HTTPS connections per worker while preserving same-origin redirect and TLS checks; classify transient network and status failures, honor bounded `Retry-After`, and use jittered backoff; derive concurrency and time ceilings from file count without weakening exact path, length, hash, or byte verification. | Fault-injection tests must cover DNS, connection reset, 408/425/429/5xx, malformed `Retry-After`, permanent byte drift, connection reuse, and the 10,000-file ceiling within the workflow budget. Complete with a current same-SHA rollback rehearsal that redeploys and verifies every exact live file, followed by the normal watchdog. |
| LAUNCH-091 | P2 | In progress | Final responsive certification found that sortable table-header buttons inherited `height:100%` from cells whose parent supplied only `min-height`, leaving the visible controls about 13 px tall at the 760 px desktop-table breakpoint and violating LAUNCH-020's 24 px minimum for visible non-inline targets. At 375 px the same measurement was a hidden-header artifact: the table head is clipped to 1 px, removed from the tab order, and replaced by the 44 px mobile sort select. | Give every `.head-sort` a 34 px minimum height matching the sticky header while retaining the mobile clipping, `tabIndex=-1`, and 44 px select replacement. Add a generated-site regression that locks all three responsive contracts. | Focused and full local suites, exact release validation, and browser geometry at 375, 768, and 1,440 px must pass before deployment. Resolve only after the exact hosted revision passes deployment, CodeQL, watchdog, production browser geometry, and a fresh accessibility audit. |
| LAUNCH-092 | P1 | Resolved | The owner home looked like a polished publication archive but still made a hedge-fund buyer infer the daily value: archive-volume proof occupied the hero while new research, cited checkpoints, captured countercase, and source health were not actionable from the first screen. Current AIMA, EY, Bloomberg, and AlphaSense product evidence consistently sells time-to-insight, monitoring, trusted attribution, joined-up workflows, and controlled AI rather than decorative dashboard density. | Recast the home as a compact decision surface with a high-contrast institutional hero, one paid-research action, direct latest-note entry, a release-derived research pulse, exact filtered paths for new-since-review/checkpoints/countercase, source-health state, evidence badges on the featured note, and clearer find/verify/private-review workflow copy. Preserve lazy loading, source boundaries, local-only review, and every publication byte. Record the external buyer evidence and the deliberately narrow product boundary in the subscription playbook. | The post-change 554-test suite, tracked-data validation, deterministic build, inline-script compilation, exact 1,157-file release validation, Python compilation, Ruff, mypy, shell/plist/diff checks, and browser interaction tests passed. Browser QA at 1,280×720 light/dark and 375×812 found zero horizontal overflow, no home target below 44 px, a 613 px mobile hero with Latest Research visible in the first viewport, and exact navigation to 17 recent records and the checkpoint lens. Exact feature revision `baea18d82c0482d61b1a0269c1a3d3fd2fd2a602` passed hosted deployment run `32982177970`, CodeQL run `32982176821`, and independent watchdog run `32991373662`. Production browser QA confirmed the same UI on data-only descendant `a43884d96244ebe3d2fc766559bd6e2899650130`: zero desktop overflow, 44 px primary actions, correct research-pulse counts, and working recent-record and checkpoint routes; its deployment run `33038227540` and CodeQL run `33038227182` also passed. |

## Severity guide

- **P0** — active security, data-integrity, or production outage risk.
- **P1** — launch-blocking functional, accessibility, performance, or deployment defect.
- **P2** — material quality or operational gap that should be fixed before launch.
- **P3** — low-risk polish or maintainability improvement.
- **BLOCKER** — cannot be safely resolved without a product decision or external owner action.
