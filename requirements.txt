# Core pipeline: stdlib only (urllib, json, re, pathlib) — no install needed.
#
# Optional: the regex+LLM hybrid direction resolver (llm_direction.py) uses the
# Anthropic SDK. It is opt-in and fail-safe — the pipeline runs fine without it.
# To enable it on the machine that runs refresh.sh:
#     pip install anthropic        # >= 0.40
#     export ANTHROPIC_API_KEY=sk-ant-...
# anthropic>=0.40   # uncomment to pin if you vendor deps
