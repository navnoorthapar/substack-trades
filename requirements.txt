# Python dependencies: none. The core pipeline uses only the Python 3.9+
# standard library; no pip install or paid API is required.
#
# Operational dependencies for automated publishing are macOS launchd, Git with
# authenticated write access to origin, Node.js for generated-script syntax
# compilation, a trusted TLS certificate bundle, and network access to Substack
# and GitHub. See README.md for installation.
#
# Optional: refresh.sh attempts the free local Ollama direction pass with
# qwen2.5:14b. If Ollama or that model is unavailable, it safely keeps regex-only
# directions. Ollama is not a Python package dependency.
